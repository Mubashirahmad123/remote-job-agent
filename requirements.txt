# Core
requests==2.32.3
beautifulsoup4==4.12.3
python-dotenv==1.2.2
feedparser==6.0.11

# AI / CrewAI
crewai==1.14.7
google-genai>=1.0.0
zhipuai>=2.1.0  # required for the GLM fallback step (Fallback 3)
# Job scraping
python-jobspy==1.1.4
playwright==1.47.0
playwright-stealth==1.0.6
crawl4ai==0.3.74

# CV parsing
pdfplumber==0.11.4
python-docx==1.1.2

# Semantic matching (optional — install for FAISS semantic scoring;
# also rebuild the Docker image afterwards, or the container stays keyword-only)
# pip install sentence-transformers faiss-cpu
# sentence-transformers>=2.2.0
# faiss-cpu>=1.7.0

# Google Sheets
gspread==6.1.0
google-auth==2.29.0

# PDF generation
reportlab==4.1.0
fpdf2==2.7.9

# Date parsing
python-dateutil==2.9.0

# Excel cleanup + formatting
openpyxl==3.1.5

# Scheduling
apscheduler==3.10.4
schedule==1.2.0

#Test
pytest

# API (Phase 1 reads)
fastapi==0.141.1
uvicorn==0.49.0
pydantic>=2.0,<3.0
httpx==0.28.1  # required for fastapi.testclient.TestClient (tests/test_api_phase1.py)
